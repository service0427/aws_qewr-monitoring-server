# Qewr Monitoring Server

## Setup
- Server IP: 13.125.105.188
- Domain: *.qewr.link
- Identity: ubuntu

## Files
- `.env`: Stores sensitive information (GitHub Token, Server IP, PEM path). **Do not commit.**
- `.gitignore`: Configured to ignore `.env` and `.pem` files.

## Connection Test
SSH connection tested successfully using `qewr.pem`.
